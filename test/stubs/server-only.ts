// Empty module — vitest swaps the real `server-only` for this one so that
// server-only code paths can be unit-tested without tripping Next.js's
// import-side runtime guard. See vitest.config.ts.
export {};
